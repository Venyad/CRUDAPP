import { AppBar } from '@mui/material'
import { Toolbar } from '@mui/material';

const NavBar = () => {
    return(
        <AppBar>
            <Toolbar>
                <p> Hello</p>

            </Toolbar>
        </AppBar>
    )
}

export default NavBar;